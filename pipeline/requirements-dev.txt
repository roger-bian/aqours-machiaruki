# Test-only dependencies, deliberately kept out of requirements.txt so
# pipeline/Dockerfile's image doesn't carry them into the Render deploy.
#
#   pip install -r pipeline/requirements-dev.txt
#   make test            (from the repo root)
#
# starlette's TestClient refuses to import without an HTTP client of its own
# (`RuntimeError: ... requires the httpx2 package`) - starlette 1.x wants
# httpx2, not httpx, and uvicorn[standard] supplies neither.
-r requirements.txt

pytest
httpx2
