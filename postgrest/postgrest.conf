# Local PostgREST config for developing against a local Postgres instead
# of Supabase - point VITE_API_BASE at this instead of the Supabase REST
# endpoint to use it.
db-uri = "postgres://web_anon:web_anon_local_dev@localhost:5432/machiaruki"
db-schema = "public"
db-anon-role = "web_anon"
server-port = 3001
